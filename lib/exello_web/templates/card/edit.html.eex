<h1>Edit Card</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.board_list_card_path(@conn, :update, @board, @list, @card)) %>

<span><%= link "Back", to: Routes.board_list_card_path(@conn, :index, @board, @list) %></span>
