<h1>New List</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.board_list_path(@conn, :create, @board)) %>

<span><%= link "Back", to: Routes.board_list_path(@conn, :index, @board) %></span>
